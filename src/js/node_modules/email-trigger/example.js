var email-trigger = require("./email-trigger.js");

email-trigger({
  user: "info@example.com",
  pass: "password",
  host: "imap.gmail.com"
}, function(mail) {
  if (/example.com/.test(mail.headers.from)) {
    console.log(mail);
  }
});
